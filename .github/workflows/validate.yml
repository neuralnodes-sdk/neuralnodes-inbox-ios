name: Validate Build

# This workflow is disabled - validation is now part of release.yml
# Keeping this file for reference only

on:
  workflow_dispatch:  # Manual trigger only

jobs:
  validate:
    runs-on: macos-latest
    
    steps:
      - name: Disabled
        run: |
          echo "This workflow is disabled. Validation is now part of release.yml"
          echo "To validate manually, trigger this workflow from the Actions tab"
